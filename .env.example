# Kokoro TTS MCP Server Configuration

# Kokoro TTS service URL
KOKORO_BASE_URL=http://localhost:8880

# Default voice to use (single voice or blended)
# Single: af_bella, af_sky, af_nicole, am_adam, am_michael, bf_emma, bf_isabella, bm_george, bm_lewis
# Blended: af_bella(2)+af_sky(1)
DEFAULT_VOICE=af_bella

# Default speech speed (0.5-2.0)
DEFAULT_SPEED=1.0

# Output directory for generated audio files
OUTPUT_DIR=~/tts_output

# Request timeout in seconds
TIMEOUT=30

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
